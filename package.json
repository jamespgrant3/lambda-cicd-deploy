{
  "name": "ts-lambda",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "watch": "tsc --watch",
    "dev": "yarn build && serverless offline start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/aws-lambda": "^8.10.10",
    "aws-lambda": "^0.1.2",
    "serverless": "^1.29.2",
    "typescript": "^3.0.1"
  },
  "devDependencies": {
    "serverless-offline": "^3.25.6"
  }
}
